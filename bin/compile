#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found a .torrc"

# if hello.txt is empty, abort the build
if [ ! -s $1/.torrc ]; then
  echo ".torrc is empty" | indent
  exit 1
fi

#replace hello with goodbye in a new file
#cat $1/.torrc |
sed -e "s/HEROKUPORT/"$PORT"/g" > $1/.torrc